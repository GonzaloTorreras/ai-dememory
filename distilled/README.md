# Distilled Context

Generated LLM context bundles are written here by `scripts/export_context.py`.

The generated Markdown files are not canonical memory. Promote durable facts
back into `memories/` after review instead of editing exports directly.
