# Session Events

Optional Codex hook metadata lands here.

Hooks should capture small review candidates or metadata only. They do not
promote durable memory and should not store raw prompts unless the user has
explicitly enabled raw capture.
