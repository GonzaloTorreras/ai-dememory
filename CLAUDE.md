# Claude Code

Read and follow `AGENTS.md`. ai DeMemory V3 has no built-in Claude hooks or
automatic capture. If an MCP connection is deliberately configured, use the
five-tool optional module and create proposals rather than canonical writes.
