# Release Acceptance Evidence

Reviewed manual release acceptance evidence is written here by:

```bash
ai-dememory acceptance record --item <id> --reviewed-by <name> --summary "..."
```

These files document human-only checks such as real MCP client use, Obsidian
inspection, provider import review, maintenance inspection, and publishing
handoff proof. They do not replace the automated release gates.
