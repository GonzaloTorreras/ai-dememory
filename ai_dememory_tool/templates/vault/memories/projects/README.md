# Project Memories

Project-specific memories live here.

The optional `ai-dememory --root <vault> onboard` flow can create reviewed
project profiles with names, aliases, paths, and recall keywords so prompt-time
retrieval can infer the active project. Operational `setup wizard` never
creates memory.
