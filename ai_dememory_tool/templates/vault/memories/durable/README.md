# Durable Memories

Reviewed durable memories live here.

Use `ai-dememory onboard` to preview minimum reviewed values, preferences, and
agent recommendations. It writes only after explicit `--apply` and reviewer
identity; later changes remain review-gated.
